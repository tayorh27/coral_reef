class BlogPost {
  String id, category, title, body, min_read, created_date;
  dynamic images, timestamp;

  BlogPost(this.id, this.category, this.title, this.body, this.min_read, this.images, this.created_date, this.timestamp);

}
